Browser preflight reference: use openTabs and claimTab. Record the error fingerprint, state change,
control surface, same action, and time budget; do not impose a fixed attempt ceiling.
For uploads, wait for the filechooser and use chooser.setFiles, confirm a file-specific compression
prompt when present, verify thumbnails and the main-image marker, and preserve the configured page.
